# 《元素之箭 · 一箭又一箭》测试报告

- 测试日期：2026-09-22
- 运行环境：Windows 10/11，Python 3.12.7，pygame-ce 2.5.8
- 自动化测试：`tests/test_acceptance.py`（unittest，13 个用例）
- 辅助测试：`tests/test_levels.py`（路径检测、六关可通关交叉验证等）
- 无头运行：通过 `SDL_VIDEODRIVER=dummy`、`SDL_AUDIODRIVER=dummy` 在不弹窗、不发声的环境下驱动真实游戏帧循环

## 一、如何运行测试

```bash
# 安装依赖
pip install pygame-ce

# Windows PowerShell 无头运行全部验收用例
$env:SDL_VIDEODRIVER="dummy"; $env:SDL_AUDIODRIVER="dummy"
python -m unittest tests.test_acceptance -v

# 或直接运行
python tests/test_acceptance.py

# 早期逻辑测试（路径检测 / 关卡可解 / 失败重开）
python tests/test_levels.py
```

实际运行结果：

```
Ran 13 tests in 0.7s
OK
```

## 二、作业规定用例 T01–T06

| 编号 | 测试内容 | 预期结果 | 实际结果 | 结论 |
| --- | --- | --- | --- | --- |
| T01 | 点击前方无阻挡的箭头（第一关 (0,1) 朝左） | 箭头飞出棋盘并消失，剩余数减 1，失误数不变 | 箭头进入 flying 状态，动画结束后从棋盘移除，剩余 6→5，失误仍为 3 | 通过 |
| T02 | 点击前方有阻挡的箭头（第一关 (4,2) 朝上，被 (2,2) 挡住） | 箭头不消失，失误次数减 1 | 箭头播放晃动动画后仍在原位，剩余数不变，失误 3→2，屏幕红闪提示 | 通过 |
| T03 | 点击位于边缘、朝向棋盘外的箭头 | 箭头正常消失，不发生越界错误 | 六个关卡逐关按解飞行，飞行过程逐帧断言所有待机箭头坐标均在棋盘范围内；第四/五/六关包含严格朝外的边缘箭头，飞出时无任何索引越界异常 | 通过 |
| T04 | 消除本关全部箭头 | 显示通关界面并可进入下一关 | 清空后弹出“关卡通过！”结算面板（星级/用时/步数），点击“下一关”后装载第二关 | 通过 |
| T05 | 失误次数耗尽 | 显示失败界面并允许重新开始 | 连续 3 次点击被挡箭头后进入“挑战失败”，点击“重新开始”后关卡重置，失误恢复为 3、箭头恢复为 6 支 | 通过 |
| T06 | 游戏进行中重新开始 | 箭头布局与失误次数恢复 | 先飞出 1 支、失误 1 次（剩余 5、失误 2），点击“重开”后布局与初始数据完全一致，失误恢复 3、步数清零 | 通过 |

## 三、扩展功能用例 T07–T13

| 编号 | 测试内容 | 预期结果 | 实际结果 | 结论 |
| --- | --- | --- | --- | --- |
| T07 | 提示按钮 | 高亮集合恰好等于当前可飞出箭头 | `Board.show_hint()` 高亮的箭头与 `removable_arrows()` 完全一致，被挡的 (4,2) 不高亮 | 通过 |
| T08 | 撤销 | 棋盘、步数、失误数恢复到上一步 | 飞出后撤销，剩余 5→6、步数 1→0、失误恢复；无历史时提示“没有可撤销的步骤” | 通过 |
| T09 | 计时与步数 | 仅游戏中累计计时，步数只统计成功飞出 | 推进 60 帧计时约 1.0s；被挡点击步数不增，成功飞出步数 +1 | 通过 |
| T10 | 随机关卡 | 生成的关卡保证可通关 | 10 个随机种子生成的 7×7、15 箭关卡 DFS 均有解；实际进入随机模式并完整通关 | 通过 |
| T11 | 自动演示 | 精灵按求解顺序自动清空棋盘 | 第三关 14 支箭头由演示功能逐支飞出，自动进入通关结算；可随时“停止演示” | 通过 |
| T12 | 星级与存档 | 零失误三星、失误一次两星，解锁进度持久化 | 零失误通关记 3 星并解锁下一关；失误一次记 2 星；重新读取 save.json 数据一致（测试使用临时存档，不污染个人进度） | 通过 |
| T13 | 选关锁定 | 未解锁关卡无法进入 | unlocked=1 时点击第 4 关无反应，点击第 1 关正常进入 | 通过 |

## 四、关卡数据验证

- `tools/gen_more_levels.py` 生成关卡时使用“逆向摆放”，并调用 DFS 求解器独立验证可通关。
- `tests/test_levels.py` 对全部 6 个关卡做两套求解逻辑交叉验证（生成顺序逆序 vs 独立 DFS），结果一致。
- 随机关卡同样必须通过求解器验证才会提供给玩家。

## 五、人工界面检查（截图核对）

在无头环境下逐界面截图人工核对，均显示正常：

1. 开始界面：标题、七元素图例、玩法说明、两个入口按钮、精灵向导“灵灵”。
2. 选关界面：6 张关卡卡片（星级/最佳用时/锁定状态）、随机试炼入口。
3. 游戏界面：HUD（时间/步数/剩余/失误）、网格点阵、元素箭头、提示金色脉动描边、环境光点。
4. 反馈：飞出星光爆发、被挡红闪与火星、通关彩屑、精灵表情与气泡提示。
5. 结算：三星依次弹出、用时步数统计、下一关；失败界面可重开或返回菜单。

## 六、结论

作业要求的 T01–T06 全部通过，扩展功能 T07–T13 全部通过，旧逻辑测试无回归。测试过程中发现并修复的问题：提示描边初版不够醒目（改为双层金色脉动光环）、结算按钮与弹窗边框重叠（重新排布）、选关锁定图标与文字重叠（调整卡片布局），均已复测通过。
